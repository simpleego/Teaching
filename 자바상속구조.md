좋아 jong, 이번에는 **상속 구조를 3단계까지 확장한 자동차 계층 구조**를 더 깊고 체계적으로 다시 설계해줄게.  
학생들이 “상속이 왜 필요한지”, “계층 구조가 어떻게 확장되는지”를 자연스럽게 이해할 수 있도록 **3단계 상속 구조**를 명확하게 보여줄게.

---

# 🚗 **상속 구조 3단계 확장 버전**

이번 구조는 다음과 같이 **3단계 계층화**를 가진다:

```
[1단계] Vehicle (탈것)
        ↓
[2단계] Car (자동차)
        ↓
[3단계] SportsCar / SUV / ElectricCar (자동차의 세부 종류)
```

이 구조는 상속의 목적을 아주 명확하게 보여줘:

- **Vehicle**: 모든 탈것의 공통 속성  
- **Car**: 자동차의 공통 속성  
- **SportsCar / SUV / ElectricCar**: 자동차 종류별 특화 기능  

---

# 🧱 **1단계: Vehicle 클래스 (최상위 부모)**

```java
public class Vehicle {
    protected String brand;
    protected int speed;

    public Vehicle(String brand) {
        this.brand = brand;
        this.speed = 0;
    }

    public void start() {
        System.out.println(brand + " 시동을 켭니다.");
    }

    public void stop() {
        speed = 0;
        System.out.println(brand + " 정지합니다.");
    }

    public void accelerate(int amount) {
        speed += amount;
        System.out.println(brand + " 속도: " + speed + "km/h");
    }
}
```

---

# 🧱 **2단계: Car 클래스 (Vehicle 상속)**

```java
public class Car extends Vehicle {
    protected int wheelCount;

    public Car(String brand, int wheelCount) {
        super(brand); // Vehicle 생성자 호출
        this.wheelCount = wheelCount;
    }

    public void honk() {
        System.out.println(brand + " 빵빵!");
    }
}
```

---

# 🧱 **3단계: 세부 자동차 클래스들**

## 🔹 SportsCar (스포츠카)

```java
public class SportsCar extends Car {
    private boolean turbo;

    public SportsCar(String brand) {
        super(brand, 4);
        this.turbo = false;
    }

    public void enableTurbo() {
        turbo = true;
        System.out.println(brand + " 터보 모드 ON!");
    }
}
```

---

## 🔹 SUV (SUV 차량)

```java
public class SUV extends Car {
    private boolean fourWheelDrive;

    public SUV(String brand) {
        super(brand, 4);
        this.fourWheelDrive = false;
    }

    public void enable4WD() {
        fourWheelDrive = true;
        System.out.println(brand + " 4륜구동 활성화!");
    }
}
```

---

## 🔹 ElectricCar (전기차)

```java
public class ElectricCar extends Car {
    private int battery;

    public ElectricCar(String brand, int battery) {
        super(brand, 4);
        this.battery = battery;
    }

    public void charge() {
        battery = 100;
        System.out.println(brand + " 배터리 100% 충전 완료!");
    }
}
```

---

# 🧪 **Main 테스트 코드**

```java
public class Main {
    public static void main(String[] args) {

        SportsCar ferrari = new SportsCar("Ferrari");
        SUV santaFe = new SUV("Hyundai SantaFe");
        ElectricCar tesla = new ElectricCar("Tesla", 80);

        ferrari.start();
        ferrari.accelerate(50);
        ferrari.enableTurbo();
        ferrari.stop();

        System.out.println("----------------");

        santaFe.start();
        santaFe.enable4WD();
        santaFe.accelerate(30);
        santaFe.stop();

        System.out.println("----------------");

        tesla.start();
        tesla.accelerate(40);
        tesla.charge();
        tesla.stop();
    }
}
```

---

# 🎯 **이 예제가 상속 교육에 좋은 이유**

| 개념 | 설명 |
|------|------|
| **3단계 계층 구조** | Vehicle → Car → SportsCar/SUV/ElectricCar |
| **super() 사용** | 부모 생성자 호출을 자연스럽게 학습 |
| **공통 기능 상속** | start(), stop(), accelerate(), honk() |
| **특화 기능 분리** | enableTurbo(), enable4WD(), charge() |
| **확장성** | 새로운 자동차 종류를 쉽게 추가 가능 |
| **현실 세계 모델링** | 학생들이 직관적으로 이해 |

---

# 🌟 jong, 더 확장해줄 수도 있어

원한다면:

- **4단계: Truck, Bus, Motorcycle 추가**  
- **추상 클래스(Abstract Class) 버전**  
- **인터페이스(Interface) 버전 (예: Chargeable, Fuelable)**  
- **다형성(Polymorphism) 예제**  
- **UML 클래스 다이어그램으로 시각화**

어떤 버전으로 확장해볼까?
