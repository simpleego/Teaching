좋아 jong, 이제 **5단계: 객체지향(OOP)으로 자판기 클래스 만들기**를 만들어볼게.  
이 단계는 학생들이 **클래스, 객체, 메서드, 캡슐화, 객체 배열**을 자연스럽게 이해하도록 설계하는 것이 핵심이야.

아래 구성은 **수업용으로 바로 사용 가능한 수준**으로 정리해줄게.

---

# ✅ **5단계 목표 (OOP 핵심 개념 학습)**

- ✅ 클래스로 데이터(음료) 정의하기  
- ✅ 클래스로 기능(자판기) 정의하기  
- ✅ 객체 배열로 메뉴 관리하기  
- ✅ 메서드로 기능 분리하기  
- ✅ main에서는 “사용”만 하도록 구조화  

---

# ✅ **📘 5단계 시나리오 (OOP 버전)**

1. **Drink 클래스**  
   - 음료 이름  
   - 가격  
   - 출력 기능(toString)

2. **VendingMachine 클래스**  
   - Drink[] 메뉴 배열  
   - 잔액 저장  
   - 메뉴 출력 기능  
   - 구매 기능  
   - 잔액 차감 기능  
   - 종료 기능

3. **Main 클래스**  
   - 돈 입력  
   - VendingMachine 객체 생성  
   - 반복문으로 메뉴 선택  

---

# ✅ **5단계 전체 코드 (OOP 구조)**

아래 코드는 **3개의 클래스로 구성된 완전한 OOP 자판기 프로그램**이야.

---

## ✅ **Drink.java**
```java
public class Drink {
    private String name;
    private int price;

    public Drink(String name, int price) {
        this.name = name;
        this.price = price;
    }

    public String getName() {
        return name;
    }

    public int getPrice() {
        return price;
    }

    @Override
    public String toString() {
        return name + " (" + price + "원)";
    }
}
```

### ✅ 해설
- 음료의 **데이터(이름, 가격)**를 담는 클래스  
- `toString()`을 오버라이드하여 출력 형식 통일  
- getter로 캡슐화 유지

---

## ✅ **VendingMachine.java**
```java
import java.util.Scanner;

public class VendingMachine {
    private Drink[] menu;
    private int money;

    public VendingMachine(int money) {
        this.money = money;

        // 메뉴 구성 (객체 배열)
        menu = new Drink[] {
            new Drink("종료", 0),
            new Drink("블랙 커피", 300),
            new Drink("믹스 커피", 300),
            new Drink("설탕 커피", 300),
            new Drink("핫초코", 500),
            new Drink("아이스티", 400)
        };
    }

    // 메뉴 출력
    public void showMenu() {
        System.out.println("\n=== 메뉴 ===");
        for (int i = 0; i < menu.length; i++) {
            System.out.println(i + ". " + menu[i]);
        }
        System.out.println("현재 잔액: " + money + "원");
    }

    // 구매 처리
    public boolean buy(int choice) {
        if (choice == 0) {
            System.out.println("프로그램을 종료합니다.");
            return false;
        }

        if (choice < 0 || choice >= menu.length) {
            System.out.println("잘못된 메뉴 번호입니다.");
            return true;
        }

        Drink d = menu[choice];

        if (money < d.getPrice()) {
            System.out.println("잔액이 부족합니다.");
            return true;
        }

        // 설탕 커피는 추가 입력
        if (d.getName().equals("설탕 커피")) {
            Scanner sc = new Scanner(System.in);
            System.out.print("설탕 스푼 수 (0~3): ");
            int sugar = sc.nextInt();

            if (sugar < 0 || sugar > 3) {
                System.out.println("설탕은 0~3 스푼만 가능합니다.");
                return true;
            }

            System.out.println("설탕 " + sugar + "스푼 커피가 준비되었습니다.");
        } else {
            System.out.println(d.getName() + "가 준비되었습니다.");
        }

        money -= d.getPrice();
        System.out.println("남은 잔액: " + money + "원");

        if (money < 300) {
            System.out.println("잔액이 부족하여 자동 종료됩니다.");
            return false;
        }

        return true;
    }
}
```

### ✅ 해설
- **객체 배열**로 메뉴 관리  
- **buy() 메서드**로 구매 기능 캡슐화  
- **showMenu() 메서드**로 메뉴 출력  
- main에서는 자판기 기능만 호출 → OOP 구조 완성

---

## ✅ **Main.java**
```java
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        System.out.print("돈을 넣어주세요: ");
        int money = sc.nextInt();

        VendingMachine vm = new VendingMachine(money);

        while (true) {
            vm.showMenu();
            System.out.print("메뉴 선택: ");
            int choice = sc.nextInt();

            boolean keepGoing = vm.buy(choice);
            if (!keepGoing) break;
        }
    }
}
```

### ✅ 해설
- main은 **입력 + 자판기 실행**만 담당  
- 모든 기능은 VendingMachine 클래스가 처리  
- OOP의 “역할 분리” 개념을 자연스럽게 학습

---

# ✅ **5단계에서 학생들이 배우는 OOP 개념**

| 개념 | 설명 |
|------|------|
| 클래스 | Drink, VendingMachine, Main |
| 객체 | new Drink(), new VendingMachine() |
| 캡슐화 | private 변수 + getter |
| 메서드 분리 | showMenu(), buy() |
| 객체 배열 | Drink[] menu |
| 역할 분리 | Main은 실행, VendingMachine은 기능 |

---

# ✅ jong, 다음 단계도 만들어줄 수 있어

원한다면:

✅ **6단계: 파일 입출력(IO)로 매출 저장하기**  
✅ **7단계: 예외 처리(try-catch) 추가하기**  
✅ **8단계: GUI 버전(스윙)으로 자판기 만들기**  
✅ **9단계: 자판기 프로젝트 통합 버전**

어떤 단계로 확장하고 싶어?
