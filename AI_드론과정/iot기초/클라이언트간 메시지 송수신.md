#  클라이언트간 메시지 송수신

>  **중앙 서버가 두 클라이언트 간의 메시지를 중계**하는 구조를 만들어 보겠습니다.
> 핵심은 서버가 클라이언트 목록을 관리하고, 클라이언트가 "상대 선택"을 하면 그 상대에게만 메시지를 전달하는 방식

---

## 📌 서버 코드 (server.py)
```python
import socket
import threading

HOST = '127.0.0.1'
PORT = 5000

clients = {}  # {conn: name}

def handle_client(conn, addr):
    # 클라이언트 이름 등록
    conn.sendall("이름을 입력하세요: ".encode())
    name = conn.recv(1024).decode().strip()
    clients[conn] = name
    print(f"[접속] {name} ({addr}) 연결됨")

    try:
        while True:
            msg = conn.recv(1024).decode()
            if not msg:
                break

            # 메시지 형식: "상대이름:메시지"
            if ":" in msg:
                target_name, message = msg.split(":", 1)
                target_conn = None
                for c, n in clients.items():
                    if n == target_name:
                        target_conn = c
                        break

                if target_conn:
                    forward = f"[{clients[conn]} -> {target_name}] {message}"
                    target_conn.sendall(forward.encode())
                    print(forward)
                else:
                    conn.sendall(f"상대 {target_name} 없음".encode())
            else:
                conn.sendall("메시지 형식: 상대이름:메시지".encode())
    except:
        pass
    finally:
        print(f"[종료] {clients[conn]} 연결 종료")
        del clients[conn]
        conn.close()

def start_server():
    server = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    server.bind((HOST, PORT))
    server.listen()
    print(f"[서버 시작] {HOST}:{PORT}")

    while True:
        conn, addr = server.accept()
        thread = threading.Thread(target=handle_client, args=(conn, addr))
        thread.start()

if __name__ == "__main__":
    start_server()
```

---

## 📌 클라이언트 코드 (client.py)
```python
import socket
import threading

HOST = '127.0.0.1'
PORT = 5000

def receive_messages(sock):
    while True:
        try:
            msg = sock.recv(1024).decode()
            if msg:
                print(f"\n{msg}")
        except:
            print("서버 연결 끊김")
            break

def start_client():
    client = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    client.connect((HOST, PORT))

    # 메시지 수신 스레드 실행
    thread = threading.Thread(target=receive_messages, args=(client,))
    thread.start()

    while True:
        msg = input()
        if msg.lower() == "exit":
            break
        client.sendall(msg.encode())

    client.close()

if __name__ == "__main__":
    start_client()
```

---

## 🚀 실행 방법
1. 서버 실행:
   ```bash
   python server.py
   ```
2. 클라이언트 실행 (2개 이상):
   ```bash
   python client.py
   ```
3. 클라이언트가 접속하면 이름을 입력합니다.  
   예: `Alice`, `Bob`  
4. 메시지 송신 형식:
   ```
   상대이름:메시지내용
   ```
   예:  
   ```
   Bob:안녕!
   ```
   → 서버가 `[Alice -> Bob] 안녕!` 형태로 중계하고 Bob 클라이언트에게만 전달합니다.

---

👉 이 구조는 **1:1 메시지 중계**를 구현한 기본형입니다.  
다음 단계에서는 이를 확장해 **여러 명이 동시에 참여하는 그룹 채팅**이나 **IoT 센서 데이터 특정 클라이언트에게만 전달**하는 구조로 발전시킬 수 있습니다.  

원하시면 제가 **3단계: 그룹 채팅 버전**도 만들어 드릴까요?
