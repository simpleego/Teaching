# 스프링부트 프로젝트 배포

---

## 1. 간단한 스프링 부트 어플리케이션 jar 파일로 생성 후


## 2. AWS EC2 서버에 배포**

## EC2 접속 설정 환경 설정
<img width="1771" height="603" alt="image" src="https://github.com/user-attachments/assets/8788f5ce-9201-4d84-b5dc-7350f2f9becc" />

```
  인스턴스 ID  
  i-02e002ee95cb675d6 (webServer)
  SSH 클라이언트를 엽니다.
  
  프라이빗 키 파일을 찾습니다. 이 인스턴스를 시작하는 데 사용되는 키는 mainkey.pem입니다.
  
  필요한 경우 이 명령을 실행하여 키를 공개적으로 볼 수 없도록 합니다.
  chmod 400 "mainkey.pem"
  
  퍼블릭 DNS을(를) 사용하여 인스턴스에 연결:
  ec2-3-27-121-60.ap-southeast-2.compute.amazonaws.com
  
  예:  
  ssh -i "mainkey.pem" ubuntu@ec2-3-27-121-60.ap-southeast-2.compute.amazonaws.com
```

### AWS 인스턴스를 설정하여 접속할 환경을 SSH로 설정한다.
> 접속에 필요한 정보를 화면에서 얻을 수 있다.

```
scp -i "키페어.pem" 보낼파일경로 사용자계정@ec2아이피:받을경로





java -version
sudo apt install openjdk-17-jre-headless

nohup java -jar file-0.0.1-SNAPSHOT.jar &
```

---
