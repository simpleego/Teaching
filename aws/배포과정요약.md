# AWS EC2 서버 상황 및 배포 과정 
- webServer1 정보
- 
--- 
ID : i-08c74fa9993fe4bba (spring-web)
SSH 클라이언트를 엽니다.

프라이빗 키 파일을 찾습니다. 이 인스턴스를 시작하는 데 사용되는 키는 spring-web.pem입니다.

필요한 경우 이 명령을 실행하여 키를 공개적으로 볼 수 없도록 합니다.



퍼블릭 DNS을(를) 사용하여 인스턴스에 연결:
## 웹사이트 임시 URL
- ec2-3-38-197-181.ap-northeast-2.compute.amazonaws.com
- ip : 3.38.197.181
--- 

# 클라이언트 PC에서 해야 할 작업

## 서버 접속 명령어
- chmod 400 "spring-web.pem"  :  파일 권한 변경
- ssh -i "spring-web.pem" ubuntu@ec2-3-38-197-181.ap-northeast-2.compute.amazonaws.com

## Java Jar파일을 서버에 upload
- scp -i springweb2-key.pem ./urbangreenProject-0.0.1-SNAPSHOT.jar ubuntu@ec2-43-203-130-198.ap-northeast-2.compute.amazonaws.com:~/


# 서버에서 해야 할 작업
- JDK 설치
- 서버 실행
- 
```
java -version
sudo apt update -- apt를 최신버전으로 업데이트 후 설치를 진행한다.
java -version -- 설치 가능한 자바 버전을 다시 확인한다.
sudo apt install openjdk-17-jre-headless  -- openjdk-17를 설치한다.

sudo nohup java -jar file-0.0.1-SNAPSHOT.jar &  -- 서버를 실행한다.
```
-----------------------------------------------------------------------------------------------------------

>> webServer2

i-0151c724b4f6ea655 (SpringWeb2)
SSH 클라이언트를 엽니다.

프라이빗 키 파일을 찾습니다. 이 인스턴스를 시작하는 데 사용되는 키는 springweb2-key.pem입니다.

필요한 경우 이 명령을 실행하여 키를 공개적으로 볼 수 없도록 합니다.
chmod 400 "springweb2-key.pem"

퍼블릭 DNS을(를) 사용하여 인스턴스에 연결:
ec2-43-203-130-198.ap-northeast-2.compute.amazonaws.com

ip: 43.203.130.198

예:

ssh -i "springweb2-key.pem" ubuntu@ec2-43-203-130-198.ap-northeast-2.compute.amazonaws.com

## 프로젝트 jar 파일을 서버에 전송
scp -i springweb2-key.pem ./urbangreenProject-0.0.1-SNAPSHOT.jar ubuntu@ec2-43-203-130-198.ap-northeast-2.compute.amazonaws.com:~/
