좋아 jong, 이제 **상속 교육을 위한 자동차 계층 구조를 4단계까지 확장**해서  
**추상 클래스 → 인터페이스 → 다형성 → UML 구조**까지 한 번에 정리해줄게.  

이 흐름은 학생들에게 **객체지향(OOP)의 핵심 개념을 자연스럽게 단계별로 이해시키는 데 최적화된 구조**야.

---

# 🚗 **[1단계] 기본 상속 구조 (Vehicle → Car → 세부 자동차)**

기본 상속 구조는 다음과 같아:

```
Vehicle (1단계)
   ↓
Car (2단계)
   ↓
SportsCar / SUV / ElectricCar (3단계)
```

여기까지는 이미 설명했으니,  
이제 **4단계 확장**을 시작해볼게.

---

# 🚚 **[2단계 확장] Truck, Bus, Motorcycle 추가 (상속 확장)**

Car를 상속받는 새로운 차량 종류를 추가해보자.

```
Vehicle
   ↓
Car
   ├── SportsCar
   ├── SUV
   ├── ElectricCar
   ├── Truck
   ├── Bus
   └── Motorcycle
```

### ✔ Truck.java
```java
public class Truck extends Car {
    private int loadCapacity;

    public Truck(String brand, int loadCapacity) {
        super(brand, 6);
        this.loadCapacity = loadCapacity;
    }

    public void loadCargo() {
        System.out.println(brand + " 화물 적재 (" + loadCapacity + "kg)");
    }
}
```

### ✔ Bus.java
```java
public class Bus extends Car {
    private int passengerCount;

    public Bus(String brand, int passengerCount) {
        super(brand, 4);
        this.passengerCount = passengerCount;
    }

    public void boardPassengers() {
        System.out.println(brand + " 승객 탑승 (" + passengerCount + "명)");
    }
}
```

### ✔ Motorcycle.java
```java
public class Motorcycle extends Car {
    public Motorcycle(String brand) {
        super(brand, 2);
    }

    public void wheelie() {
        System.out.println(brand + " 휠리!");
    }
}
```

---

# 🧱 **[3단계] 추상 클래스(Abstract Class) 적용**

Car는 더 이상 “구체적인 자동차”가 아니라  
“자동차의 공통 특징을 정의하는 추상 개념”이므로  
**추상 클래스**로 만드는 것이 자연스러워.

### ✔ Car를 추상 클래스로 변경

```java
public abstract class Car extends Vehicle {
    protected int wheelCount;

    public Car(String brand, int wheelCount) {
        super(brand);
        this.wheelCount = wheelCount;
    }

    public void honk() {
        System.out.println(brand + " 빵빵!");
    }

    // 모든 자동차는 drive()를 가져야 하지만
    // 종류마다 방식이 다르므로 추상 메서드로 선언
    public abstract void drive();
}
```

### ✔ 자식 클래스에서 drive() 구현

예: SportsCar

```java
@Override
public void drive() {
    System.out.println(brand + " 스포츠 주행!");
}
```

---

# 🔌 **[4단계] 인터페이스(Interface) 적용**

자동차 종류에 따라  
- 전기차는 충전 가능  
- 휘발유차는 주유 가능  
- 버스는 승객 탑승 가능  

이런 “능력(기능)”을 **인터페이스로 분리**하면 훨씬 유연해져.

---

## ✔ Chargeable (충전 가능)

```java
public interface Chargeable {
    void charge();
}
```

## ✔ Fuelable (주유 가능)

```java
public interface Fuelable {
    void refuel();
}
```

---

## ✔ ElectricCar implements Chargeable

```java
public class ElectricCar extends Car implements Chargeable {
    private int battery;

    public ElectricCar(String brand, int battery) {
        super(brand, 4);
        this.battery = battery;
    }

    @Override
    public void charge() {
        battery = 100;
        System.out.println(brand + " 배터리 충전 완료!");
    }

    @Override
    public void drive() {
        System.out.println(brand + " 전기차 주행!");
    }
}
```

---

## ✔ GasolineCar implements Fuelable

```java
public class GasolineCar extends Car implements Fuelable {
    private int fuel;

    public GasolineCar(String brand, int fuel) {
        super(brand, 4);
        this.fuel = fuel;
    }

    @Override
    public void refuel() {
        fuel = 100;
        System.out.println(brand + " 주유 완료!");
    }

    @Override
    public void drive() {
        System.out.println(brand + " 휘발유차 주행!");
    }
}
```

---

# 🎭 **[5단계] 다형성(Polymorphism) 예제**

다형성은 “부모 타입으로 자식 객체를 다루는 것”.

### ✔ Car 타입으로 다양한 자동차를 다루기

```java
Car[] cars = {
    new SportsCar("Ferrari"),
    new SUV("SantaFe"),
    new ElectricCar("Tesla", 80),
    new Truck("Volvo", 5000),
    new Bus("Hyundai Bus", 45),
    new Motorcycle("Harley")
};

for (Car c : cars) {
    c.start();
    c.drive();   // 다형성의 핵심
    c.stop();
    System.out.println();
}
```

### ✔ 인터페이스 기반 다형성

```java
Chargeable[] chargers = {
    new ElectricCar("Tesla", 80),
    new ElectricCar("Ioniq", 60)
};

for (Chargeable c : chargers) {
    c.charge();
}
```

---

# 📐 **[6단계] UML 클래스 다이어그램**

텍스트 기반 UML로 시각화해줄게.

```
                <<abstract>>
                Vehicle
                + brand
                + speed
                + start()
                + stop()
                + accelerate()

                        ▲
                        │
                <<abstract>>
                    Car
                + wheelCount
                + honk()
                + drive() <<abstract>>

        ┌──────────────┼──────────────┬──────────────┬──────────────┐
        │              │              │              │              │
 SportsCar         SUV         ElectricCar      GasolineCar     Motorcycle
+ turbo            + 4WD       + battery        + fuel          + wheelie()
+ enableTurbo()    + enable4WD() + charge()     + refuel()      + drive()
+ drive()          + drive()   + drive()        + drive()

Interfaces:
Chargeable
+ charge()

Fuelable
+ refuel()
```

---

# 🎉 **정리**

| 단계 | 내용 |
|------|------|
| **1단계** | 기본 상속 구조 Vehicle → Car → 세부 자동차 |
| **2단계** | Truck, Bus, Motorcycle 추가로 상속 확장 |
| **3단계** | Car를 추상 클래스(Abstract Class)로 변경 |
| **4단계** | Chargeable, Fuelable 인터페이스 적용 |
| **5단계** | 다형성(Polymorphism) 예제 |
| **6단계** | UML 클래스 다이어그램으로 시각화 |

---

jong, 필요하면  
- **코드 전체 통합 버전**  
- **실습 문제 세트**  
- **학생용 PPT 형태 요약**  
- **UML 이미지 버전**  

이런 것도 만들어줄 수 있어.
